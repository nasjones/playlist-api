CREATE TABLE Playlist_Records(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    playlist_id INTEGER
        REFERENCES Playlists(id) ON DELETE CASCADE NOT NULL,
    genre_id INTEGER
        REFERENCES Genres(id) ON DELETE CASCADE NOT NULL
);